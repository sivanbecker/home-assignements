# Internals
> Generated by /ts-code-viewer on 2026-05-31

## Class Diagram

```mermaid
classDiagram
  class Config {
    +port number
    +host string
    +logLevel string
  }
  class PostLemoBody {
    +name string
    +value string
  }
  class PostLemoResponse {
    +received true
    +name string
    +value string
  }
  class controllers {
    +getLemo() void
    +postLemo(body PostLemoBody) PostLemoResponse
  }
  class handlers {
    +getLemoHandler(req, reply) Promise~void~
    +postLemoHandler(req, reply) Promise~void~
  }
  class routes {
    +registerRoutes(app FastifyInstance) void
  }
  class app {
    +buildApp() Promise~FastifyInstance~
  }
  class schemas {
    +getLemoSchema FastifySchema
    +postLemoBodySchema FastifySchema
  }

  app --> routes : registers
  routes --> handlers : wires
  routes --> schemas : attaches
  handlers --> controllers : delegates
  controllers ..> PostLemoBody : consumes
  controllers ..> PostLemoResponse : produces
  app ..> Config : reads via server.ts
```

- `handlers.ts` is the center of gravity for HTTP concerns — it bridges Fastify types to pure logic
- `controllers.ts` has zero Fastify imports — correct boundary, makes unit testing trivial
- `schemas.ts` is the single source of truth for both validation and OpenAPI output
- No class is too broad; each module is ≤2 exported functions

---

## Call-Flow: getLemoHandler

```mermaid
flowchart TD
  client["Client / inject()"] --> getLemoHandler
  getLemoHandler --> getLemo["controllers.getLemo()"]
  getLemo --> void["returns void"]
  void --> reply200["reply.code(200).send()"]
  getLemoHandler -->|unexpected throw| errorHandler["errorHandler → 500 JSON"]
```

- `getLemo()` is intentionally a no-op for the placeholder — replace with real logic on interview day
- `reply.code(200).send()` with no argument produces an empty 200 body, as required
- Error path goes to `setErrorHandler` — the handler itself has no try/catch

---

## Call-Flow: postLemoHandler

```mermaid
flowchart TD
  client["Client / inject()"] --> fastifyValidation{Fastify JSON\nschema valid?}
  fastifyValidation -->|no - missing/wrong fields| reply400["400 Bad Request\n{ statusCode, error, message }"]
  fastifyValidation -->|yes| postLemoHandler
  postLemoHandler --> postLemo["controllers.postLemo(body)"]
  postLemo --> buildResponse["{received:true, name, value}"]
  buildResponse --> replySend["reply.send(result) → 200 JSON"]
  postLemoHandler -->|unexpected throw| errorHandler["errorHandler → 500 JSON"]
```

- Validation happens before the handler is called — handlers never see invalid bodies
- `postLemo()` is a pure function with no side effects — safe to unit-test without mocks
- Extra fields are silently stripped by AJV (`removeAdditional` is Fastify's default)

---

## Sequence: Main Path — POST /lemo

```mermaid
sequenceDiagram
  participant Test as Test / curl
  participant Fastify as Fastify Core
  participant AJV as AJV (schema validation)
  participant H as handlers.ts
  participant C as controllers.ts

  Test->>Fastify: POST /lemo { name, value }
  Fastify->>AJV: validate body against postLemoBodySchema
  AJV-->>Fastify: valid ✓
  Fastify->>H: postLemoHandler(request, reply)
  H->>C: postLemo(request.body)
  C-->>H: { received: true, name, value }
  H->>Fastify: reply.send(result)
  Fastify-->>Test: 200 { received: true, name, value }
```

---

## Review Slices

### 1. Entry-Point Slice
**Review question:** Does the public entry point stay thin and delegate correctly?

```mermaid
flowchart LR
  test["tests/routes.test.ts"] --> buildApp["app.ts · buildApp()"]
  buildApp --> routes["routes.ts · registerRoutes()"]
  routes --> handlers["handlers.ts"]
  handlers --> controllers["controllers.ts"]
  controllers --> types["schemas.ts · PostLemoBody"]
```

- `buildApp()` is the only entry point used in tests — no real port opened
- Each layer adds exactly one concern: wiring → HTTP bridge → pure logic
- `schemas.ts` is imported only by `routes.ts` (for attachment) and type-only in `handlers.ts`
- **Clean:** no test directly imports controllers or handlers — all HTTP behavior tested via `inject()`

---

### 2. Success-Path Slice
**Review question:** Does the happy path flow cleanly through all layers with no leakage?

```mermaid
sequenceDiagram
  participant R as routes.test.ts
  participant A as app.ts
  participant Rt as routes.ts
  participant H as handlers.ts
  participant C as controllers.ts

  R->>A: buildApp()
  A->>Rt: registerRoutes(app)
  R->>A: inject POST /lemo {name,value}
  A->>H: postLemoHandler
  H->>C: postLemo(body)
  C-->>H: PostLemoResponse
  H-->>R: 200 { received:true, name, value }
```

- No shared mutable state — `postLemo()` is a pure function
- Response shape is determined in `controllers.ts`, not in `handlers.ts` — correct separation
- **Clean:** Fastify types never leak below `handlers.ts`

---

### 3. Failure-Path Slice
**Review question:** Are all failure branches handled and returning consistent error shapes?

```mermaid
flowchart TD
  request["Incoming request"] --> validation{Schema valid?}
  validation -->|missing field| f400["400 — Fastify default\n{statusCode,error,message}"]
  validation -->|valid| handler["Handler executes"]
  handler -->|success| f200["200 — JSON response"]
  handler -->|unexpected throw| errH["errorHandler.ts"]
  errH --> f500["500 — {statusCode,error,message}"]
  unknownRoute["Unknown route"] --> f404["404 — Fastify default\n{statusCode,error,message}"]
```

- All error shapes use the same `{ statusCode, error, message }` envelope — consistent for API consumers
- 400 (validation) is handled by Fastify's built-in AJV pipeline — no custom code needed
- 500 (unexpected) is handled by `registerErrorHandler` in `errors/errorHandler.ts`
- `errorHandler` intentionally does NOT catch 400s — letting Fastify's default flow through keeps error shapes consistent

---

### 4. Boundary-Risk Slice
**Review question:** Are layer boundaries clean — do lower layers ever import from higher layers?

```mermaid
flowchart TD
  server["server.ts\n(Entrypoint)"]
  app["app.ts\n(Factory)"]
  infra["plugins/ · errors/\n(Infrastructure)"]
  http["routes · handlers · schemas\n(HTTP Layer)"]
  domain["controllers.ts\n(Domain)"]

  server -->|imports| app
  app -->|imports| infra
  app -->|imports| http
  http -->|imports| domain
  domain -.->|"✅ no upward imports"| http
  infra -.->|"✅ no upward imports"| app
```

- **No boundary violations.** All imports flow strictly downward.
- `controllers.ts` imports zero Fastify types — domain logic is framework-agnostic
- `config.ts` is imported only by `plugins/logging.ts` and `server.ts` — not scattered across layers
- **Worth noting in interview:** this boundary discipline means swapping Fastify for Express would only touch `handlers.ts`, `routes.ts`, `app.ts`, and the plugins — domain logic survives intact
