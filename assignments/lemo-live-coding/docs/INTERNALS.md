# Internals
> Generated by /ts-code-viewer — 2026-06-03

## Class Diagram

_Structural — shows full exported API surface of the onboarding domain._

```mermaid
classDiagram
  class SessionStore {
    -sessions Map~string, Session~
    -ttlMs number
    +create() Session
    +get(sessionId) Session
    +update(sessionId, patch) Session
    +advanceStep(sessionId, step) Session
  }
  class QuoteEngine {
    -factors QuoteFactors
    +calculate(session) Quote
  }
  class QuoteFactors {
    +perCarFactors PricingFactor[]
    +totalFactors TotalPricingFactor[]
  }
  class PricingFactor {
    <<interface>>
    +apply(session, car) number
  }
  class ProfilePricingFactor {
    <<interface>>
  }
  class CarPricingFactor {
    <<interface>>
  }
  class TotalPricingFactor {
    <<interface>>
    +applyToTotal(session) number
  }
  class AgeFactor {
    +apply(session, _car) number
  }
  class CarCountFactor {
    +applyToTotal(session) number
  }
  class WebError {
    +statusCode number
    +message string
  }
  class OnboardingWebError
  class SessionNotFoundError
  class SessionExpiredError
  class StepAlreadyDoneError
  class StepPrerequisiteError

  PricingFactor <|-- ProfilePricingFactor
  PricingFactor <|-- CarPricingFactor
  ProfilePricingFactor <|.. AgeFactor
  TotalPricingFactor <|.. CarCountFactor
  QuoteEngine --> QuoteFactors
  QuoteEngine ..> Session : reads
  SessionStore ..> Session : manages
  Error <|-- WebError
  WebError <|-- OnboardingWebError
  Error <|-- SessionNotFoundError
  Error <|-- SessionExpiredError
  Error <|-- StepAlreadyDoneError
  Error <|-- StepPrerequisiteError
```

- **Center of gravity:** `SessionStore` — all 5 controller functions pass through it
- **QuoteEngine** is deliberately narrow — reads session, returns Quote, no side effects
- **Error hierarchy** is the most interesting design choice — 4 domain errors + 1 HTTP error, clean separation
- `update()` type-restricts patch with `Omit<..., 'sessionId'|'createdAt'|'step'>` — immutable identity fields enforced by the type system

---

## Call-Flow: SessionStore.get

```mermaid
flowchart TD
  get[get sessionId] --> lookup{Map.get}
  lookup -->|undefined| notFound[throw SessionNotFoundError]
  lookup -->|found| ttl{TTL elapsed?}
  ttl -->|yes| expired[throw SessionExpiredError]
  ttl -->|no| returnSession[return Session]
```

## Call-Flow: SessionStore.advanceStep

```mermaid
flowchart TD
  adv[advanceStep sessionId step] --> lookup{Map.get}
  lookup -->|undefined| notFound[throw SessionNotFoundError]
  lookup -->|found| order{new step index > current?}
  order -->|no| alreadyDone[throw StepAlreadyDoneError]
  order -->|yes| advance[spread + set new step]
  advance --> returnSession[return updated Session]
```

## Call-Flow: QuoteEngine.calculate

```mermaid
flowchart TD
  calc[calculate session] --> init[selectedCarIds / eligibleCars from session]
  init --> loop{for each carId}
  loop --> findCar{car in eligibleCars?}
  findCar -->|no| skip[skip — continue]
  findCar -->|yes| perCar[reduce perCarFactors × BASE_PREMIUM]
  perCar --> round[Math.round → perCar map]
  round --> subtotal[add to subtotal]
  subtotal --> loop
  loop -->|done| totalMult[reduce totalFactors → multiplier]
  totalMult --> total[Math.round subtotal × multiplier]
  total --> returnQuote[return Quote]
```

## Call-Flow: controllers.submitQuote

```mermaid
flowchart TD
  sq[submitQuote store engine sessionId body] --> get[store.get sessionId]
  get -->|SessionNotFoundError| bubble1[bubble to handler → 404]
  get -->|SessionExpiredError| bubble2[bubble to handler → 410]
  get -->|ok| require[requireStep PROFILED]
  require -->|StepAlreadyDoneError| bubble3[bubble to handler → 409]
  require -->|StepPrerequisiteError| bubble4[bubble to handler → 422]
  require -->|ok| updateIds[store.update selectedCarIds]
  updateIds --> getAgain[store.get — re-fetch with carIds]
  getAgain --> calc[engine.calculate]
  calc --> updateQuote[store.update quote]
  updateQuote --> advance[store.advanceStep QUOTED]
  advance --> returnSession[return Session]
```

---

## Sequence: Full Happy Path (POST /quote)

```mermaid
sequenceDiagram
  participant Test as Test / Client
  participant R as routes.ts
  participant H as onboarding/handlers.ts
  participant C as onboarding/controllers.ts
  participant S as SessionStore
  participant E as QuoteEngine

  Test->>R: POST /onboarding/:sessionId/quote
  R->>H: quoteHandler(request, reply)
  H->>C: submitQuote(store, engine, sessionId, body)
  C->>S: get(sessionId)
  S-->>C: Session
  C->>S: update(sessionId, {selectedCarIds})
  C->>S: get(sessionId)
  S-->>C: Session with carIds
  C->>E: calculate(session)
  E-->>C: Quote
  C->>S: update(sessionId, {quote})
  C->>S: advanceStep(sessionId, QUOTED)
  S-->>C: Session
  C-->>H: Session
  H-->>R: reply.send(session)
  R-->>Test: 200 + {step: QUOTED, quote: {...}}
```

---

## Review Slices

### 1. Entry-Point Slice

**Review question:** Is the public API surface minimal and correctly layered?

```mermaid
flowchart TD
  test["routes.test.ts\nfastify.inject()"] --> routes["routes.ts\nregisterRoutes()"]
  routes --> handlers["onboarding/handlers.ts\nquoteHandler()"]
  handlers --> controllers["onboarding/controllers.ts\nsubmitQuote()"]
  controllers --> store["SessionStore\ncreate/get/update/advanceStep"]
  controllers --> engine["QuoteEngine\ncalculate()"]
  store --> types["types.ts\nSession, SessionStep"]
  engine --> types
```

- Tests only import `buildApp()` — no test reaches into domain directly via routes
- Route registration is the only place that knows about URL patterns
- Handlers are the only layer that knows about HTTP status codes (via `mapDomainError`)
- Controllers are pure functions — no Fastify types anywhere in domain

### 2. Success-Path Slice

**Review question:** Does the happy path flow cleanly without unnecessary steps?

```mermaid
flowchart LR
  start[POST /start] -->|session STARTED| profile[POST /profile]
  profile -->|eligibleCars attached, PROFILED| quote[POST /quote]
  quote -->|quote calculated, QUOTED| bind[POST /bind]
  bind -->|boundAt set, BOUND| done[GET /:id → BOUND]
```

- Each step is idempotent within a session — the store is the source of truth
- `submitQuote` calls `store.get()` twice — once to check step, once after update to pass fresh session to engine. Slight overhead, but keeps logic explicit.

### 3. Failure-Path Slice

**Review question:** Are all error branches handled consistently?

```mermaid
flowchart TD
  handler[Any handler] --> try{try controller}
  try -->|SessionNotFoundError| e404[OnboardingWebError 404]
  try -->|SessionExpiredError| e410[OnboardingWebError 410]
  try -->|StepAlreadyDoneError| e409[OnboardingWebError 409]
  try -->|StepPrerequisiteError| e422[OnboardingWebError 422]
  try -->|unknown| rethrow[re-throw — errorHandler → 500]
  e404 --> errHandler[errorHandler → response]
  e409 --> errHandler
  e410 --> errHandler
  e422 --> errHandler
  rethrow --> errHandler
```

- `mapDomainError` is defined once in `handlers.ts` and called in every handler — DRY, consistent
- Unknown errors re-throw, hitting the Fastify `errorHandler` → 500 — no silent swallowing
- The 409/422 distinction is encoded in two distinct error classes, not a string check

### 4. Boundary-Risk Slice

**Review question:** Are there any cross-layer violations or unexpected dependencies?

```mermaid
graph LR
  domain["Domain Layer\ncontrollers / SessionStore / QuoteEngine"]
  http["HTTP Layer\nhandlers / schemas / routes"]
  infra["Infra\nWebError / errorHandler"]
  types["Data Model\ntypes.ts"]

  http --> domain
  http --> infra
  domain --> types
  domain -->|"only: OnboardingWebError extends WebError"| infra

  style domain fill:#e8f5e9
  style http fill:#e3f2fd
  style infra fill:#fce4ec
  style types fill:#f3e5f5
```

- **Clean boundary** — domain only crosses into infra for `OnboardingWebError extends WebError`, which is intentional by design
- Domain has zero Fastify imports — fully portable
- The one discussion point: `SessionStore` and `QuoteEngine` singletons in `handlers.ts`. At scale, move construction to `buildApp()` and pass via Fastify decorators
